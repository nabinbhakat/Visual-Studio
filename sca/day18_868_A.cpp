#include<bits/stdc++.h>
using namespace std; 
int32_t main(){
   ios_base::sync_with_stdio(false);
   cin.tie(NULL);
   cout.tie(NULL);
   string pass;
   cin>>pass;
   int n; 
   cin>>pass;
   string s,x;
   for(int i=0;i<n;i++){
   		cin>>x;
   		s+=x;
    }
    cout<<s;
    // if(s.find(pass))
    // 	cout<<"YES";
    // else
    // 	cout<<"NO";
   return 0;
}
